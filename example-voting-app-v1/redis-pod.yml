appVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: voting-app-pod
    app: example-voting-app
spec:
  container:
    - name: voting-app
      image: redis
      ports:
        - containerPort: 6379
