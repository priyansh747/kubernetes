appVersion: v2
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: redis-pod
    app: example-voting-app
spec:
  containers:
    - name: voting-app
      image: redis
      ports:
        - containerPort: 6379
