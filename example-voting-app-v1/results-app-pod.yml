appVersion: v1
kind: Pod
metadata:
  name: result-app-pod
  labels:
    name: voting-app-pod
    app: example-voting-app
spec:
  container:
    - name: result-app
      image: prabhavagrawal/result-app
      ports:
        - containerPort: 80
