apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    name: postgres-pod
    app: example-voting-app
spec:
  containers:
    - name: db
      image: postgres
      env:
        - name: POSTGRES_PASSWORD
          value: mysecretpassword
      ports:
        - containerPort: 5432
